package homewor1;


import org.junit.Test;
import org.junit.Assert;

public class TEstClass {

    @Test
    public void test(){
        Assert.assertTrue(true);
        Assert.assertTrue(true);
        Assert.assertTrue(true);
        Assert.assertTrue(true);
    }
}
